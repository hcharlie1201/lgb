<div id="conversation-#{@conversation.id}" phx-update="stream" phx-viewport-top="load_more">
  <div :for={{dom_id, conversation} <- @streams.conversations} id={dom_id}>
    <p>{conversation.sender_id} {conversation.receiver_id}</p>
  </div>
</div>
